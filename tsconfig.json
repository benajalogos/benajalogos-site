{
  "extends": "astro/tsconfigs/strict",

  "compilerOptions": {
    "baseUrl": ".",

    /* Nodig voor Node APIs zoals process, fs, path */
    "types": ["node"],

    /* Handige imports */
    "paths": {
      "@layouts/*": ["src/layouts/*"],
      "@components/*": ["src/components/*"]
    }
  },

  /* Astro + alle bestanden meenemen */
  "include": [
    ".astro/types.d.ts",
    "**/*"
  ],

  /* Build output uitsluiten */
  "exclude": [
    "dist"
  ]
}